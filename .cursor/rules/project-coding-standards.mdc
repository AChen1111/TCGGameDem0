---
description: Project coding standards for all code edits
alwaysApply: true
---

编码时必须遵守：

- 代码尽量精简：最小正确改动；不加无关注释、包装或防御性噪音。
- 边界条件：允许报错；可加可不加的边界处理一律不加。
- Lua 命名：成员 `m_类型+变量名`（如 `m_nCount`、`m_strName`、`m_bReady`、`m_btnConfirm`）；私有函数以 `_` 开头（如 `_updateView`）。
- Lua EmmyLua：编写/修改 Lua 必须加 `---@class` / `---@field` / `---@param` / `---@return`。
- Lua C# 别名：禁止业务 Lua 写 `CS.xxx`；重命名写进 `Assets/Scripts/LuaRaw/Include.lua`，业务只用别名。
- 编码时按需使用 `.cursor/skills/`：先走 `using-agent-skills` 路由，再读对应 `SKILL.md`（实现/测试/调试/Unity/提交等）。
- 不确定就问，不要猜着实现；提问用选项弹窗（`AskQuestion`），推荐项放第一个并标注「(推荐)」，不要在正文里罗列选项。
- 每次对用户的回复末尾列出本次调用的 skills：`Skills: skill-a, skill-b, ...`
